---
- name: install journald-forward
  copy:
    src: journald-forward.service
    dest: /etc/systemd/system/

- name: reload systemd
  systemd:
    daemon_reload: yes
  ignore_errors: yes

- name: enable journald forwarding
  service:
    name: journald-forward
    enabled: yes

- name: start journald forwarding
  service:
    name: journald-forward
    state: started
  ignore_errors: yes


