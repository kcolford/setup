#!/bin/bash
set -euo pipefail

case "$(basename "$(basename "$(basename "$TERM" color)" 256)" -)" in
    xterm)
	term=xterm
	;;
    screen)
	term=screen
	;;
    *)
	exit 1
	;;
esac

nohup $term > /dev/null 2>&1 &
